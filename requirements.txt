# Runtime dependencies. The Torch HPC Singularity overlay
# /scratch/jv2807/dms_singularity/dms_contrastive.ext3 already provides most of
# these (torch 2.5.1+cu121, transformers 4.48.1, fair-esm, peft 0.18.1, h5py,
# pyarrow, wandb, scikit-learn, pandas, matplotlib, seaborn). Only umap-learn is
# typically missing and must be installed into the overlay (see env/build_overlay.md).
torch>=2.5
transformers>=4.48
fair-esm>=2.0
peft>=0.15
h5py>=3.10
pyarrow>=14
pandas>=2.0
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
umap-learn>=0.5
tqdm>=4.65
wandb>=0.16
